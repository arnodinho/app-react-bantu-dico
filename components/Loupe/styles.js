import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  loupe: {
    marginTop: 5,
    paddingTop: 35,
    flex: 1,
    backgroundColor: '#eee',
    justifyContent: 'flex-start',
    alignItems: 'center',
  },
});
